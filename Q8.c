#include<stdio.h>
int main(){
    int arr[5],i,n;
    printf("Enter the 5 elements:\n");
    for(i=0;i<5;i++){
        scanf("%d",&arr[i]);
    }
    printf("Enter the search element:");
    scanf("%d",&n);

    for(i=0;i<5;i++){
        if(n==arr[i]);
       

    }
     printf("%d is at %d position",n,&arr[n])
}