#include<stdio.h>
int main(){
    
    char name,yrname;
    int  passwd;
    printf("Enter your name:");
    scanf("%s",&yrname);
    printf("Enter your Username:");
    scanf("%s",name);
    printf("Enter your password:");
    scanf("%d",passwd);                    // only for number password for now.
    if(name==(scanf("%d",name)) && passwd==(scanf("%d",passwd))){
        printf("Welcome Back",yrname);
    }
}


//complete it and then push in github